apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: dev-gateway-p2p
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-gateway-p2p
  template:
    metadata:
      labels:
        app: dev-gateway-p2p
    spec:
      imagePullSecrets:
        - name: gcrio
      containers:
        - name: dev-gateway-p2p
          image: ${CICD_IMAGE}:${CICD_EXECUTION_SEQUENCE}
          ports:
            - containerPort: 80
      #     volumeMounts:
      #       - mountPath: /tmp2
      #         name: env
      # volumes:
      #   - configMap:
      #       defaultMode: 256
      #       name: dev-env
      #       optional: false
      #     name: env
