/productType/init:
  post:
    security:
      - bearerAuth: []
    tags:
      - productType
    summary: init productType meta info
    description: return productTypeIds
    consumes:
      - application/json
    produces:
      - application/json
    requestBody:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ProductType'
          example:
            - type: PF
              text: ''
              comment: pf
            - type: BRIDGE
              text: ''
              comment: bridge
    responses:
      200:
        description: productTypeIds
